// JavaScript String substring()

// substring() is similar to slice().

// The difference is that start and end values less than 0 are treated as 0 in substr
let str = "Apple, Banana, Kiwi";
let x = str.substring(7, 13);
console.log(x);